<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <materialDesign:PackIconKind x:Key="EXIT_APP_ICON">Power</materialDesign:PackIconKind>
    <materialDesign:PackIconKind x:Key="MINIMALIZE_ICON">WindowMinimize</materialDesign:PackIconKind>
    <materialDesign:PackIconKind x:Key="CHECK_ICON">Check</materialDesign:PackIconKind>
    <materialDesign:PackIconKind x:Key="EDIT_ICON">Pencil</materialDesign:PackIconKind>
    <materialDesign:PackIconKind x:Key="SAVE_ICON">ContentSave</materialDesign:PackIconKind>
    <materialDesign:PackIconKind x:Key="CANCEL_ICON">Cancel</materialDesign:PackIconKind>
    <materialDesign:PackIconKind x:Key="CLOSE_ICON">Close</materialDesign:PackIconKind>
    <materialDesign:PackIconKind x:Key="RUBLE_ICON">Ruble</materialDesign:PackIconKind>

</ResourceDictionary>
